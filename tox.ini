[tox]
env_list =
    py310
    py311
    py312
minversion = 4.0.16

[testenv]
description = Run the tests and calculate coverage.
package = editable
extras = test,release
deps =
    coverage
    pytest>=7.0.0, <=8.0.0
commands =
    pytest
    coverage run -m pytest
    coverage html
